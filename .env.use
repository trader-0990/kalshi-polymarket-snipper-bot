# Kalshi API (required for authenticated calls)
KALSHI_API_KEY=

# Private key: use either file path or PEM string
KALSHI_PRIVATE_KEY_PEM=
# KALSHI_PRIVATE_KEY_PEM="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# Set to true for demo environment (demo-api.kalshi.co)
KALSHI_DEMO=false

POLYMARKET_PRIVATE_KEY= 
POLYMARKET_PROXY=0x0CE0f0B103a240340E014797E8d8d65846F5C89c            
# this is the arbitrage target address
RPC_URL=https://cosmopolitan-wandering-diamond.matic.quiknode.pro/6bf670a49dc63da6be4d1400e1144f37e9aeaedf









# --- Arb (cross-platform: buy when sum in [ARB_SUM_LOW, ARB_SUM_THRESHOLD)) ---
ARB_SUM_THRESHOLD=0.5      # upper bound; opportunity when sum < this
ARB_SUM_LOW=0.75            # lower bound; ignore if sum < this (e.g. bad data)
ARB_PRICE_BUFFER=0.01        # limit price = captured ask + this (better fill)
ARB_SIZE=5                    # same token amount on both (Kalshi contracts = Poly shares); default 5
ARB_KALSHI_MIN=5             # min Kalshi contracts per order (default 5)
ARB_POLY_MIN=5               # min Polymarket shares per order (default 5)
ARB_DRY_RUN=false             # if set, only log opportunity and orders, do not place
ARB_POLY_PLACE_RETRY_LIMIT=1  # max times to allow placing a new order for same ticker/leg when Poly failed (default 5)


ARB_MAX_DIVERGENCE=0.35
ARB_MIN_SIDE_PROB=0.10

POLY_BUY_MIN=0.85
POLY_SELL_BELOW=0.8